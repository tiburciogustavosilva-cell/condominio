[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: qualquer rota cai no index.html (React Router cuida do resto no cliente).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
