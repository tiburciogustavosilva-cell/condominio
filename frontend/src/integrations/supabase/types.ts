/**
 * Types gerados a partir do schema do Supabase.
 *
 * Ainda não existe projeto Supabase neste app — este é um placeholder.
 * Quando o projeto for criado e as migrations (ver /supabase/migrations)
 * estiverem aplicadas, regenere este arquivo com:
 *
 *   npx supabase gen types typescript --project-id <seu-project-id> > src/integrations/supabase/types.ts
 *
 * Não edite esse arquivo manualmente depois de gerado.
 */
export type Database = Record<string, unknown>;
