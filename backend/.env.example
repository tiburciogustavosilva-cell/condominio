# Porta do servidor da API
PORT=4000

# Postgres (usado pelo Prisma)
DATABASE_URL="postgresql://postgres:SENHA@localhost:5432/aquila?schema=public"

# Chave que assina os tokens JWT (Bearer). Gere com: openssl rand -hex 32
JWT_SECRET=
JWT_EXPIRES_IN=7d

# ------------------------------------------------------------------
# E-mail dos lembretes de manutenção.
# Sem SMTP_HOST, o envio é SIMULADO: o e-mail não sai, só aparece no
# console do backend. Preencha para enviar de verdade.
# ------------------------------------------------------------------
# SMTP_HOST=smtp.seuprovedor.com
# SMTP_PORT=587
# SMTP_USER=usuario@seudominio.com
# SMTP_PASS=sua_senha_ou_app_password
# MAIL_FROM=Condomínio <nao-responder@seudominio.com>
