{
  "name": "condominio-sistema",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de gestao de condominio (frontend React/Vite + Supabase)",
  "scripts": {
    "postinstall": "npm --prefix frontend install",
    "setup": "npm --prefix frontend install",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "dev:backend-legado": "npm --prefix backend install && npm --prefix backend run dev"
  }
}
